# Meeting Rooms I & II

**Тема:** sorting + интервалы · **Сложность:** easy → medium · **Приоритет:** ⚪ дополнительная

## Условие

**Часть I (easy).** Дан массив интервалов встреч `intervals[i] = [start, end]`.
Верни `true`, если человек может посетить **все** встречи (то есть никакие две не пересекаются).

**Часть II (medium).** Верни **минимальное число переговорок**, необходимое, чтобы провести все
встречи.

Встреча `[1, 5]` и встреча `[5, 8]` **не** конфликтуют: одна заканчивается ровно тогда, когда
начинается другая.

## Примеры

```
Часть I:
  [[0,30], [5,10], [15,20]]   → false     // [0,30] пересекается с [5,10]
  [[7,10], [2,4]]             → true

Часть II:
  [[0,30], [5,10], [15,20]]   → 2
  [[7,10], [2,4]]             → 1
  [[1,5], [5,8]]              → 1         // касание не требует второй комнаты
```

## Ограничения

- `0 <= intervals.length <= 10^4`
- `0 <= start < end <= 10^6`

## 🎯 Цель по сложности

- Часть I: `O(N log N)` время, `O(1)` дополнительной памяти.
- Часть II: `O(N log N)` время, `O(N)` память.

## 🧠 Ментальная модель

**Часть I.** Отсортировать по началу и проверить соседей: если начало следующей встречи раньше
конца предыдущей — конфликт. Достаточно смотреть только на **соседей**, потому что после
сортировки все более ранние встречи начинаются ещё левее.

**Часть II — приём «два отсортированных массива событий».**
Разделяем начала и концы и сортируем их **независимо**:

```
starts: [0, 5, 15]
ends:   [10, 20, 30]
```

Идём по началам. Если очередное начало раньше самого раннего свободного конца — нужна **новая**
комната. Иначе комната освободилась — переиспользуем её и двигаем указатель по концам.

```
start=0  vs end=10 → 0 < 10  → нужна комната, rooms = 1
start=5  vs end=10 → 5 < 10  → нужна комната, rooms = 2
start=15 vs end=10 → 15 >= 10 → комната освободилась, endIndex++
итог: 2 ✅
```

Максимальное значение `rooms` = максимальное число **одновременно** идущих встреч.

## ⚠️ Подводные камни именно этой задачи

1. **Начала и концы сортируются независимо.** Это выглядит «неправильно» — мы разрываем пары
   `[start, end]`. Но нам и не нужно знать, какая именно встреча закончилась; важно **сколько**
   их идёт одновременно. Осознай этот момент — он самый неинтуитивный в задаче.
2. **Касание — не конфликт.** Условие `start < end` строгое (не `<=`). Для `[[1,5],[5,8]]`
   ответ `1`, а не `2`.
3. **Компаратор обязателен** при сортировке чисел и массивов.
4. **Часть II нельзя решать «максимум пересечений с каждым»** — это `O(N²)`.
5. Пустой массив: часть I → `true`, часть II → `0`.

## 💡 Подсказки (открывай по очереди)

<details>
<summary>Подсказка 1 — часть I</summary>

```js
intervals.sort((a, b) => a[0] - b[0]);
for (let i = 1; i < intervals.length; i++) {
  if (intervals[i][0] < intervals[i - 1][1]) return false;
}
return true;
```

</details>

<details>
<summary>Подсказка 2 — часть II, структура</summary>

Два массива: `starts` и `ends`, оба отсортированы. Один указатель по каждому.

</details>

<details>
<summary>Подсказка 3 — часть II, логика</summary>

Идём по `starts`. `if (starts[i] < ends[endIndex]) rooms++; else endIndex++;`
Максимум `rooms` за всё время — ответ.

</details>

---

## ✍️ Моё решение

```js
function canAttendMeetings(intervals) {
  // часть I
}

function minMeetingRooms(intervals) {
  // часть II
}
```

## 🧮 Моя оценка сложности

Часть I: время O(?) · память O(?)
Часть II: время O(?) · память O(?)

---

## 🔍 Разбор — открывай ТОЛЬКО после своей попытки

<details>
<summary>Часть I — решение</summary>

```js
function canAttendMeetings(intervals) {
  intervals.sort((a, b) => a[0] - b[0]);

  for (let i = 1; i < intervals.length; i++) {
    if (intervals[i][0] < intervals[i - 1][1]) return false;   // строгое <: касание ОК
  }

  return true;
}
```

Время `O(N log N)` (сортировка), память `O(1)` дополнительно.

</details>

<details>
<summary>Часть II — решение (два массива событий)</summary>

```js
function minMeetingRooms(intervals) {
  if (intervals.length === 0) return 0;

  const starts = intervals.map(i => i[0]).sort((a, b) => a - b);
  const ends   = intervals.map(i => i[1]).sort((a, b) => a - b);

  let rooms = 0;
  let maxRooms = 0;
  let endIndex = 0;

  for (let i = 0; i < starts.length; i++) {
    if (starts[i] < ends[endIndex]) {
      rooms++;                       // встреча началась, свободных комнат нет
    } else {
      endIndex++;                    // одна освободилась — переиспользуем (rooms не растёт)
    }
    maxRooms = Math.max(maxRooms, rooms);
  }

  return maxRooms;
}
```

**Трассировка** `[[0,30], [5,10], [15,20]]`:
`starts = [0, 5, 15]`, `ends = [10, 20, 30]`

| i | starts[i] | ends[endIndex] | сравнение | rooms | maxRooms |
|:-:|:---------:|:--------------:|---|:-----:|:--------:|
| 0 | 0 | 10 | `0 < 10` | 1 | 1 |
| 1 | 5 | 10 | `5 < 10` | 2 | 2 |
| 2 | 15 | 10 | `15 >= 10` → endIndex = 1 | 2 | 2 |

Ответ **2** ✅

**Сложность:** время `O(N log N)` — две сортировки; память `O(N)` — два массива.

</details>

<details>
<summary>Альтернатива: sweep line (разностный массив событий)</summary>

```js
function minMeetingRoomsSweep(intervals) {
  const events = [];
  for (const [start, end] of intervals) {
    events.push([start, 1]);      // встреча началась
    events.push([end, -1]);       // встреча закончилась
  }

  // при равном времени сначала обрабатываем ОКОНЧАНИЯ (-1 < +1)
  events.sort((a, b) => a[0] - b[0] || a[1] - b[1]);

  let rooms = 0;
  let maxRooms = 0;
  for (const [, delta] of events) {
    rooms += delta;
    maxRooms = Math.max(maxRooms, rooms);
  }
  return maxRooms;
}
```

Приём «sweep line» (заметающая прямая) обобщается на массу задач: «максимум машин на дороге»,
«сколько людей в здании одновременно», «Car Pooling».

⚠️ Тонкость в компараторе: `a[0] - b[0] || a[1] - b[1]` — при одинаковом времени окончание
(`-1`) идёт раньше начала (`+1`), иначе касание `[1,5]` и `[5,8]` посчитается как пересечение.
Приём `x || y` в компараторе (сравнить по второму ключу, если первый дал `0`) стоит запомнить.

</details>
